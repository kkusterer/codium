use add_to_repo
add_to_repo is an alias for git add . && git commit -m "."
alias located in the .zshrc file


1. use ctrl+s to save the courent files
2. type add_to_repo
3. type git push origin main
4. check the repo 3 miniuts later and see if it updated


corect order w/o add_to_repo
1 use ctrl+s to save the courent files
2 git add .
3 git commit -m "."
4 git push origin main
5 check the repo


for python vm
python3 -m venv venv
then source venv/bin/activate
then pip install --upgrade pip
then pip install (exe flask)
then run progrma
then deactivate to deactivate

runflask is flask run --host=0.0.0.0


ctrl + s on file to save
cd to codium dir
git status
git add .
git commit -m "."
git push origin main
git status


sql 
SELECT * FROM responses;
SELECT team_number, drive_type, can_climb FROM responses;
SELECT * FROM responses
    WHERE can_climb = 'Yes';
SELECT * FROM responses
    ORDER BY team_number ASC;
SELECT COUNT(*) FROM responses;

Show best auto scorers:

SELECT team_number, auto_average
FROM responses
ORDER BY auto_average DESC;


Find all swerve teams:

SELECT team_number FROM responses
WHERE drive_type = 'Swerve';

enter: sqlite3 survey.db

exit : .quit

DELETE FROM responses; = DELETE everything

